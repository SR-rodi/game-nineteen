package ru.sr.nineteen.itemlist

/*
class RandomItemList: CreateItemList() {
    override fun create(): MutableList<GameItemEngine> {
            val item = baseItem
            val itemList = mutableListOf<GameItemEngine>()
            for (i in 0 until baseItem.size){
                val position = Random.nextInt(0..item.lastIndex)
                itemList.add(GameItemEngine(item[position], StatusItem.NOT_CHOICE))
                item.removeAt(position)
            }
            return itemList

    }
}*/
